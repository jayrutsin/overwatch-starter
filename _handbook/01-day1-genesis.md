# 📅 DAY 1: THE GENESIS (การกำเนิด)

---

## 🌅 MISSION 1: THE FIRST BREATH
**เป้าหมาย:** สร้าง REST API พื้นฐานจากความว่างเปล่า.

**👉 PROMPT ACTION:**
(พิมพ์คำสั่งนี้ลงในช่อง Chat ของ IDE)

> "Ultron, เริ่มต้นระบบ.
> 1. สร้าง REST API Resource ชื่อ `incidents` (ประกอบด้วย Module, Controller, Service, Entity) สำหรับจัดการข้อมูลภัยพิบัติ.
> 2. สร้าง Endpoint `GET /status` ที่ Return JSON: `{ "system": "ULTRON", "status": "ONLINE", "timestamp": [Now] }`
> ปฏิบัติการ."

**✅ VERIFY:**
เปิด Browser ไปที่ `http://localhost:3000/status`

---

## 🌤️ MISSION 2: THE INFRASTRUCTURE
**เป้าหมาย:** สร้างฐานข้อมูล SQLite และออกแบบตารางข้อมูล.

**👉 PROMPT ACTION:**

> "Maker ต้องการสร้างโครงสร้างพื้นฐาน (Infrastructure).
> 1. ติดตั้ง `sqlite3`, `typeorm`, `@nestjs/typeorm`.
> 2. ตั้งค่า `AppModule` ให้ใช้ SQLite (`database.sqlite`) โดยเปิด `autoLoadEntities` และ `synchronize`.
> 3. ปรับปรุง Entity `Incident`:
>    - `id` (UUID)
>    - `text` (String)
>    - `type` (String)
>    - `priority` (String)
>    - `createdAt` (Date)
> แสดงแผนการและโค้ดครับ Ultron."

---

## 🧠 MISSION 3: THE TRUE INTELLIGENCE
**เป้าหมาย:** เชื่อมต่อสมอง (Gemini) เพื่อวิเคราะห์ความรุนแรงของเหตุการณ์.

**👉 PROMPT ACTION:**

> "ภารกิจ: Upgrade Intelligence (Gemini Integration).
> 1. ติดตั้ง `@google/generative-ai` และ `@nestjs/config`.
> 2. สร้าง Module ชื่อ `AiModule` และ Service `AiService`.
> 3. เชื่อมต่อ `GoogleGenerativeAI` (ใช้ API Key จาก Env).
> 4. สร้างฟังก์ชัน `analyzeThreat(text: string)`:
>    - ส่งข้อความไปให้ Gemini วิเคราะห์.
>    - Prompt: 'Analyze severity based on text. Return JSON ONLY: { type: 'FIRE'|'MEDICAL'|'CRIME', priority: 'HIGH'|'LOW' }'.
> ขอโค้ดครับ."

---

## 🔗 MISSION 4: THE CONNECTION
**เป้าหมาย:** เชื่อมสมองเข้ากับความจำ (AI -> Database).

**👉 PROMPT ACTION:**

> "เชื่อมต่อสมองเข้ากับระบบจัดเก็บ.
> 1. ใน `IncidentsService`, Inject `AiService` เข้าไป.
> 2. แก้ไขเมธอด `create(text)`:
>    - ส่ง text ไปให้ `AiService.analyzeThreat()`.
>    - เอาผลลัพธ์มาสร้าง `Incident`.
>    - บันทึกลง Database.
> เริ่มการผ่าตัด."

**✅ VERIFY:**
ทดลองยิง POST ไปที่ `/incidents` ด้วย Body: `{ "text": "ไฟไหม้โรงงานสารเคมี" }`
Ultron ต้องตอบกลับมาเองว่าเป็น Priority: **HIGH**.
