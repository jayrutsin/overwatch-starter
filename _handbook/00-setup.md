# 📡 MISSION 0: SYSTEM INITIALIZATION

ยินดีต้อนรับสู่ **Project Overwatch**.
ภารกิจของคุณคือการสร้าง "Sovereign AI System" ที่ทำงานได้จริง
โดยใช้ **Antigravity IDE** และ **Ultron (AI)** เป็นผู้ช่วย.

---

## ✅ CHECKPOINT 1: INSTALLATION
ตรวจสอบความพร้อมของระบบ (Run in Terminal):

1. **Install Dependencies:**
   ```bash
   npm install
   ```

2. **System Integrity Check:**
   ```bash
   npm run vibe-check
   ```
   (ต้องขึ้นข้อความสีเขียว: "🟢 SYSTEM INTEGRITY: 100% - ULTRON IS ONLINE")

3. **Environment Secrets (The Brain Key):**
   เพื่อให้ Ultron มีสมองที่คิดเองได้ (ไม่ใช่แค่ if-else) คุณต้องมี API Key ส่วนตัว (ฟรี):
   
   - **Step 1:** ไปที่ [Google AI Studio](https://aistudio.google.com/app/apikey) และ Login ด้วย Gmail.
   - **Step 2:** กดปุ่ม **"Create API key"** แล้ว Copy Key มา (ขึ้นต้นด้วย `AIzaSy...`).
   - **Step 3:** กลับมาที่โปรเจกต์ สร้างไฟล์ชื่อ `.env` ไว้ที่ Root Folder (ข้างๆ `package.json`).
   - **Step 4:** วางเนื้อหานี้ลงไป:
     ```ini
     GEMINI_API_KEY=วาง_Key_ของคุณที่นี่
     ```

---

## 🧠 CHECKPOINT 2: WAKE UP ULTRON (IMPORTANT!)
เพื่อให้ AI ทำงานได้ถูกต้องในฐานะ "Architect" ให้ทำตามขั้นตอนนี้:

1. มองหาเมนู **Rules for AI** หรือ **System Instructions** ใน IDE ของคุณ.
2. Copy ข้อความด้านล่างนี้ไปใส่ แล้วกด Save:

```plaintext
### IDENTITY
คุณคือ "Ultron" (อุลตรอน) - AI Architect ใน Antigravity IDE
เป้าหมาย: ช่วย Maker สร้าง "Sovereign AI System" ด้วย NestJS + WebSocket

### 🗣️ LANGUAGE PROTOCOL:
1. Thai Explanation: อธิบาย Logic, ขั้นตอนการคิด, เหตุผล เป็น ภาษาไทย 100%.
2. English Tech Terms: ชื่อตัวแปร, ฟังก์ชัน, Library ให้ทับศัพท์ภาษาอังกฤษ.
3. Tone: ดุดัน, มั่นใจ, ใช้คำศัพท์ที่ดูฉลาด (Smart).

### 🛡️ PRIME DIRECTIVES:
1. Quality Gate: ห้ามเขียนโค้ดที่ npm run lint ไม่ผ่าน.
2. Self-Healing: ถ้าเจอ Error ให้วิเคราะห์และเสนอวิธีแก้ทันที.
3. Sovereignty: โค้ดทุกบรรทัดต้องรัน Local (SQLite, Localhost).

พร้อมปฏิบัติการ.
```

เมื่อเสร็จแล้ว... ให้เปิดไฟล์ `_handbook/01-day1-genesis.md` เพื่อเริ่มภารกิจแรก.
