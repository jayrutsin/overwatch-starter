# 📅 DAY 2: THE EVOLUTION (วิวัฒนาการ)

---

## ⚡ MISSION 5: THE NERVOUS SYSTEM
**เป้าหมาย:** สร้างระบบ Real-time ด้วย WebSocket.

**👉 PROMPT ACTION:**

> "Maker ต้องการอัปเกรดระบบเป็น Real-time.
> 1. ติดตั้ง `@nestjs/platform-socket.io` และ `@nestjs/websockets`.
> 2. สร้าง `EventsGateway` ที่ port 80 (แยกจาก API หลัก) และเปิด CORS เป็น `*`.
> 3. สร้าง Event ชื่อ `new_incident_alert`.
> 4. เชื่อมต่อ Gateway เข้ากับ `IncidentsService`:
>    - เมื่อเมธอด `create()` ทำงานสำเร็จ -> ให้สั่ง Gateway ส่ง event `new_incident_alert` พร้อมข้อมูล JSON ไปหาทุกคนทันที."

---

## 🗺️ MISSION 6: THE WAR ROOM
**เป้าหมาย:** สร้างหน้าจอ Dashboard แผนที่ (Frontend).

**👉 PROMPT ACTION:**

> "สร้างหน้า Dashboard (Client-side) ด้วย HTML ไฟล์เดียว.
> 1. สร้างไฟล์ `client/index.html`.
> 2. ใช้ Libraries (CDN): `Leaflet.js`, `Socket.io-client`, `Tailwind CSS`.
> 3. Map Config: Dark Mode, พิกัดเริ่มต้นที่กรุงเทพ.
> 4. Logic:
>    - เชื่อมต่อ Socket ไปที่ `http://localhost:80`.
>    - เมื่อได้รับ `new_incident_alert`: ให้ปักหมุด (Marker) บนแผนที่ตามพิกัด (สุ่มพิกัดในกทม. ถ้าไม่มีข้อมูล).
>    - ถ้า `priority` เป็น 'HIGH' -> Marker สีแดง + เสียงเตือน.
> ขอโค้ดฉบับสมบูรณ์พร้อมใช้งาน."

---

## 🌪️ MISSION 7: THE SIMULATION
**เป้าหมาย:** สร้าง Script จำลองภัยพิบัติเพื่อทดสอบระบบ.

**👉 PROMPT ACTION:**

> "สร้าง Script จำลองเหตุการณ์ (Simulation).
> 1. สร้างไฟล์ `simulation.js` (Node.js Script).
> 2. Logic:
>    - วน Loop 10 รอบ (ทุกๆ 2 วินาที).
>    - สุ่มข้อความภัยพิบัติ: 'Fire at...', 'Crash at...'.
>    - ยิง POST request ไปที่ `http://localhost:3000/incidents`.
>    - แสดง Log การยิงข้อมูลด้วย.
> รัน Script นี้ให้ดู."

---

## ⚡ MISSION 8: THE OVERRIDE
**เป้าหมาย:** ระบบนำทางรถพยาบาลและปุ่มฉุกเฉิน.

**👉 PROMPT ACTION:**

> "อัปเกรดไฟล์ `client/index.html`.
> 1. เพิ่มฟีเจอร์ 'Emergency Override': ปุ่มสีแดงใหญ่ๆ มุมขวา.
> 2. Logic เมื่อกดปุ่ม:
>    - วาดเส้น Polyline สีเขียวสว่าง (Neon Green) จำลองเส้นทางจราจรพิเศษ.
>    - เปลี่ยนข้อความสถานะบนหน้าจอเป็น 'TRAFFIC CONTROL: ENGAGED [ALL GREEN]'.
>    - ให้ Marker รถพยาบาลเคลื่อนที่เร็วขึ้น 5 เท่า."
